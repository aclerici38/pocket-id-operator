{{- if and .Values.rbacHelpers .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: pocket-id-operator
    name: pocketidusergroup-viewer-role
rules:
    - apiGroups:
        - pocketid.internal
      resources:
        - pocketidusergroups
      verbs:
        - get
        - list
        - watch
    - apiGroups:
        - pocketid.internal
      resources:
        - pocketidusergroups/status
      verbs:
        - get
{{- end }}
