{{- if and .Values.rbacHelpers .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: pocket-id-operator
    name: pocketidoidcclient-viewer-role
rules:
    - apiGroups:
        - pocketid.internal
      resources:
        - pocketidoidcclients
      verbs:
        - get
        - list
        - watch
    - apiGroups:
        - pocketid.internal
      resources:
        - pocketidoidcclients/status
      verbs:
        - get
{{- end }}
