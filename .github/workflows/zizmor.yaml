name: Zizmor

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
  pull_request:
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
  workflow_dispatch:

jobs:
  zizmor:
    uses: aclerici38/workflows/.github/workflows/zizmor.yaml@812a0ac85f6264fef2c88842473423f72e251dca # main
    permissions:
      contents: read
      security-events: write
